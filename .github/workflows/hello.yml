name: Hello Shrimp

on:
  workflow_dispatch:
    inputs:
      task:
        description: '想叫蝦蝦做什麼'
        required: true
        default: '幫我整理今天的重點'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Show task
        run: echo "收到任務：${{ github.event.inputs.task }}"
